PORT=8080

# Generate a strong secret: openssl rand -hex 32
JWT_SECRET=change-this-to-a-long-random-secret-before-deploying

# Postgres (not needed until you swap the in-memory repos)
# DATABASE_URL=postgres://user:password@localhost:5432/golearn?sslmode=disable